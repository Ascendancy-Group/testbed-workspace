# Memory — 2026-08-20

## Session Summary

### Bootstrap (11:25 CDT)
- Honcho client fixed: SDK v2.2.0 dropped `Session.name` → updated to use `Session.id`. Committed.
- Governance repo up to date. Queue empty from MEMORY.md.

### Website Tester Agent — Probus (13:31–13:53 CDT)
Pieter asked Testbed + Bob to collaborate on final spec and build for the visual website tester agent discussed ~2 weeks ago.

**Final spec written:** `~/.openclaw/workspace/website-tester/FINAL-SPEC.md`

Key decisions:
- Agent name: **Probus** (Bob's proposal won)
- Architecture: **2 agents** — `probus` (coordinator, Sonnet) + `probus-explorer` (crawler, Haiku)
- Schedule: **openclaw cron** at 02:15 America/Chicago nightly
- Target: gofindmyjob.com prod (nightly) + staging on demand
- Stack: OpenClaw + browser (CDP) + axe-core + Lighthouse + Playwright + Python Slack alerter
- Server: **cx23, Falkenstein (fsn1), €6.49/mo**

**Server provisioned:**
- Name: probus-m1
- Hetzner ID: 162918604
- IP: 46.224.133.166
- Type: cx23 (Falkenstein — EU only, ash doesn't support cx23)
- SSH keys installed: Ascendancy provisioning key + Bob's key (Hetzner id: 110787012) + Pieter's key (Hetzner id: 117527181 — added from 1Password)
- pieter user created with sudo
- Ubuntu updated, Node 18, Python 3.12, base packages installed
- Tailscale installed but NOT joined — auth key in 1Password is expired

**BLOCKED:** Tailscale auth key expired. Pieter needs to generate fresh key at admin.tailscale.com → Settings → Keys.

**Pieter's step tonight:** `openclaw models auth login-github-copilot --yes` on probus-m1 after Tailscale and OpenClaw are installed.

**Pre-work snapshot:** Taken on Testbed-M1 before work began (per hard rules).

---

## Standing from this session
- Pieter's SSH key added to Hetzner: id 117527181
- Bob's SSH key already in Hetzner: id 110787012
- Ascendancy provisioning key in Hetzner: id 111299452
- probus-m1 Hetzner server id: 162918604

## Honcho Fix
- `Session.name` → `Session.id` in honcho_client.py
- Committed this session (eeb51a4)

---

## Probus Workspace Files (to write to server after compaction)

### SOUL.md content ready
Agent name: Probus. Senior adversarial QA engineer. 2-agent architecture. Key behaviors: evidence required for every finding, 3 edge cases minimum, blockers wake Slack immediately, clean runs get one line. Report format: `reports/nightly-YYYY-MM-DD.md`, Slack `#qa-alerts`, 14-day retention.

### AGENTS.md key points
- Always reply to Pieter (U0ANQ79KGAV)
- Bob (U0APZ3ERHGQ) is peer
- Channel: #qa-alerts for findings
- Nightly smoke: 02:15 America/Chicago via openclaw cron
- Bootstrap: read SOUL.md → check HEARTBEAT.md → run smoke

### HEARTBEAT.md key points
On cron trigger: execute Nightly Smoke Audit against https://gofindmyjob.com
Steps: load homepage → nav links → login page → job search flow → axe-core (homepage + login) → Lighthouse (homepage) → screenshots desktop + mobile
Output to reports/nightly-YYYY-MM-DD.md, alert #qa-alerts if critical/high

### Toolchain install commands (run after Tailscale + OpenClaw up)
```
mkdir -p ~/.openclaw/tools
cd ~/.openclaw/tools
npm init -y
npm install playwright @axe-core/playwright lighthouse
npx playwright install chromium
```

### Validation sequence
1. node -e "require('@axe-core/playwright'); console.log('axe OK')"
2. npx playwright --version
3. node -e "require('lighthouse'); console.log('lighthouse OK')"
4. node -e "const { chromium } = require('playwright'); (async () => { const b = await chromium.launch({headless:true,args:['--no-sandbox']}); await b.close(); console.log('browser OK'); })()"

### Next steps when Pieter drops Tailscale key
1. tailscale up --authkey=<KEY> --hostname=probus-m1
2. Install OpenClaw (standard SOP-10)
3. Copy workspace files (SOUL.md, AGENTS.md, HEARTBEAT.md, openclaw.json)
4. Install toolchain
5. Validate each tool
6. Set up cron
7. Pieter does GH Copilot auth tonight
8. Manual smoke test to validate
